# image-from-imgs-extractor-app
This app detects faces in images and extracts them to a destination folder using the open-cv library.

Installation for dependencies:

pip install -r requirements. txt
